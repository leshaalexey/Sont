# Одна команда на всё: демон, CLI и окно в трее вместе с его фронтендом.
#
# Обычные `cargo build` и `cargo test` окно не трогают — оно вне
# `default-members`, потому что Tauri тянет несколько сотен зависимостей и
# замедлил бы каждый прогон тестов в разы.
[alias]
app = "build --workspace"
