<!--
  Вырез между плашками шапки. В макете это отдельная фигура, потому что
  плашки — прямоугольники, а стык между ними скруглён внутрь.
-->
<script>
  let { left = null, inner = false } = $props();
</script>

<svg
  class="notch"
  class:inner
  style={left != null ? `left:${left}px` : undefined}
  width="6"
  height="5"
  viewBox="0 0 6 5"
>
  <!--
    Вырезы — половинки окружности радиусом в полторы точки.

    Было по одной, и на плашке в пятнадцать точек это читалось зазубриной, а
    не скруглением: стык плашек выглядел надколотым. Полторы точки дают
    настоящую дугу — перемычка между плашками сужается плавно.
  -->
  <path
    d="M 1.5 0 L 0 0 L 0 5 L 1.5 5 C 1.5 4.172 2.172 3.5 3 3.5 C 3.828 3.5 4.5 4.172 4.5 5 L 6 5 L 6 0 L 4.5 0 C 4.5 0.828 3.828 1.5 3 1.5 C 2.172 1.5 1.5 0.828 1.5 0 Z"
  />
</svg>

<style>
  /* Цвет тот же, что у плашек: перемычка — их продолжение, а не отдельная
     фигура. Пока она была кремовой намертво, включённый цвет системы
     оставлял между цветными плашками белые перепонки; пока она брала акцент —
     в светлой теме между белыми плашками появлялись чернильные. */
  .notch {
    position: absolute;
    top: 9px;
    fill: var(--raised);
  }
  /* Вырез внутри плашки живёт в её системе координат. */
  .inner {
    left: -4px;
    top: 5px;
  }
</style>
